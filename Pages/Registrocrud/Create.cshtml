@page
@model RegistroMat2.Pages.Registrocrud.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>registro</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="registro.fecharegistro" class="control-label"></label>
                <input asp-for="registro.fecharegistro" class="form-control" />
                <span asp-validation-for="registro.fecharegistro" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="registro.estudianteID" class="control-label"></label>
                <select asp-for="registro.estudianteID" class ="form-control" asp-items="ViewBag.estudianteID"></select>
            </div>
            <div class="form-group">
                <label asp-for="registro.maestroID" class="control-label"></label>
                <select asp-for="registro.maestroID" class ="form-control" asp-items="ViewBag.maestroID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
