@page
@model RegistroMat2.Pages.Registrocrud.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>registro</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.registro.fecharegistro)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.registro.fecharegistro)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.registro.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
