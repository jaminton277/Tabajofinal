@page
@model RegistroMat2.Pages.Seccioncrud.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>seccion</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="seccion.ID" />
            <div class="form-group">
                <label asp-for="seccion.ubicacion" class="control-label"></label>
                <input asp-for="seccion.ubicacion" class="form-control" />
                <span asp-validation-for="seccion.ubicacion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="seccion.EscuelaID" class="control-label"></label>
                <select asp-for="seccion.EscuelaID" class="form-control" asp-items="ViewBag.EscuelaID"></select>
                <span asp-validation-for="seccion.EscuelaID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
